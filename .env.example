# Base URL for the API server. Leave empty in local dev — vite.config.ts proxies
# /api to http://localhost:4000, so a relative path works with zero CORS setup.
# In production (Netlify), set this to your deployed Render API URL.
VITE_API_BASE_URL=https://crm-practice-api.onrender.com
